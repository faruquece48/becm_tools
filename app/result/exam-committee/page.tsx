"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { Pencil, Plus, Trash2 } from "lucide-react";
import { formatTabulatorDate } from "@/lib/storage/tabulators";
import { loadExamCommittees, saveExamCommittees, type ExamCommitteeRecord } from "@/lib/storage/examCommittees";

const headings = ["Sl.", "Exam Type", "Exam Year", "Academic Year", "Semester", "Chairman", "Member 1", "Member 2", "Member 3", "Member 4 (External)", "Exam Date", "Result Publish Date", "Memo No", "Memo Date", "Result Note", "Edit", "Delete"];

export default function ExamCommitteePage() {
  const [records, setRecords] = useState<ExamCommitteeRecord[]>([]);
  const [search, setSearch] = useState("");
  const [pageSize, setPageSize] = useState(10);
  const [page, setPage] = useState(1);
  useEffect(() => { const timer = window.setTimeout(() => { loadExamCommittees().then(setRecords).catch((error) => window.alert(error instanceof Error ? error.message : "Unable to load exam committees")); }, 0); return () => window.clearTimeout(timer); }, []);
  const filtered = useMemo(() => { const query = search.trim().toLowerCase(); return query ? records.filter((record) => Object.values(record).some((value) => value.toLowerCase().includes(query))) : records; }, [records, search]);
  const pageCount = Math.max(1, Math.ceil(filtered.length / pageSize));
  const currentPage = Math.min(page, pageCount);
  const visible = filtered.slice((currentPage - 1) * pageSize, currentPage * pageSize);
  const first = filtered.length ? (currentPage - 1) * pageSize + 1 : 0;
  const last = Math.min(currentPage * pageSize, filtered.length);
  const remove = async (record: ExamCommitteeRecord) => { if (!window.confirm(`Delete the ${record.examYear} ${record.academicYear} exam committee?`)) return; const updated = records.filter((item) => item.id !== record.id); try { const saved = await saveExamCommittees(updated); setRecords(saved); } catch (error) { window.alert(error instanceof Error ? error.message : "Unable to delete exam committee"); } };

  return <div className="min-h-screen bg-slate-50 p-4 sm:p-7"><div className="mx-auto max-w-[1800px]"><div className="flex justify-end py-3"><Link href="/result/exam-committee/new" className="inline-flex items-center gap-2 rounded-lg bg-sky-500 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-sky-600"><Plus className="h-4 w-4" /> Add Exam Committee</Link></div><section className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b p-4 sm:flex-row sm:items-center sm:justify-between"><label className="flex items-center gap-2 text-sm font-semibold">Show <select value={pageSize} onChange={(event) => { setPageSize(Number(event.target.value)); setPage(1); }} className="rounded-lg border px-3 py-2">{[10,25,50].map((size) => <option key={size}>{size}</option>)}</select> entries</label><label className="flex items-center gap-2 text-sm font-semibold">Search: <input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1); }} className="w-56 rounded-lg border px-3 py-2 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" /></label></div><div className="w-full overflow-x-hidden"><table className="w-full table-fixed border-collapse text-xs min-[1900px]:table-auto"><thead className="bg-[#082f57] text-white"><tr>{headings.map((heading) => <th key={heading} className="break-words border-r border-white/20 px-1.5 py-3 text-center font-bold whitespace-normal min-[1900px]:whitespace-nowrap">{heading}</th>)}</tr></thead><tbody>{visible.map((record,index) => <tr key={record.id} className="border-b odd:bg-white even:bg-slate-50 hover:bg-indigo-50/50"><td className="break-words px-1.5 py-3 text-center align-top whitespace-normal min-[1900px]:whitespace-nowrap">{(currentPage-1)*pageSize+index+1}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.examType}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.examYear}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.academicYear}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.semester}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.chairman}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.member1}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.member2}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.member3}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.member4}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{formatTabulatorDate(record.examDate)}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{formatTabulatorDate(record.resultPublishDate)}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.memoNo}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{formatTabulatorDate(record.memoDate)}</td><td className="break-words px-1.5 py-3 align-top whitespace-normal min-[1900px]:whitespace-nowrap">{record.resultNote}</td><td className="break-words px-1.5 py-3 text-center align-top whitespace-normal min-[1900px]:whitespace-nowrap"><Link href={`/result/exam-committee/${record.id}/edit`} aria-label="Edit exam committee" className="inline-flex rounded-lg bg-emerald-500 p-2.5 text-white hover:bg-emerald-600"><Pencil className="h-4 w-4" /></Link></td><td className="break-words px-1.5 py-3 text-center align-top whitespace-normal min-[1900px]:whitespace-nowrap"><button type="button" onClick={() => remove(record)} aria-label="Delete exam committee" className="inline-flex rounded-lg bg-red-500 p-2.5 text-white hover:bg-red-600"><Trash2 className="h-4 w-4" /></button></td></tr>)}{!visible.length && <tr><td colSpan={17} className="px-4 py-12 text-center text-slate-500">No exam committee records found.</td></tr>}</tbody></table></div><div className="flex flex-col gap-4 p-4 text-sm sm:flex-row sm:items-center sm:justify-between"><p>Showing {first} to {last} of {filtered.length} entries</p><div className="flex flex-wrap gap-2"><button disabled={currentPage===1} onClick={() => setPage((value) => Math.max(1,value-1))} className="rounded-lg border px-3 py-2 disabled:opacity-40">Previous</button>{Array.from({length:pageCount},(_,index)=>index+1).map((number)=><button key={number} onClick={()=>setPage(number)} className={`h-9 min-w-9 rounded-lg border px-3 ${number===currentPage?"border-[#082f57] bg-[#082f57] text-white":"bg-white hover:bg-slate-50"}`}>{number}</button>)}<button disabled={currentPage===pageCount} onClick={() => setPage((value) => Math.min(pageCount,value+1))} className="rounded-lg border px-3 py-2 disabled:opacity-40">Next</button></div></div></section></div></div>;
}